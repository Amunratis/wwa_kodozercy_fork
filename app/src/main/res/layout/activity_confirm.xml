<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.findly.application.confirmActivity.ConfirmActivity">ł

    <ImageView
        android:scaleType="centerCrop"
        android:id="@+id/activityConfirmResult"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <ImageView
        android:id="@+id/activityConfirmCloseIv"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_gravity="top|left"
        android:layout_marginLeft="20dp"
        android:layout_marginTop="20dp"
        android:src="@drawable/ic_close"
        android:tint="@android:color/white" />

    <ImageView
        android:id="@+id/activityConfirmResultSend"
        android:layout_width="65dp"
        android:layout_height="65dp"
        android:layout_gravity="center|right|bottom"
        android:text="CONFIRM"
        android:scaleType="fitCenter"
        android:layout_margin="22dp"

        android:background="@drawable/send_icon"/>
</android.support.design.widget.CoordinatorLayout>
